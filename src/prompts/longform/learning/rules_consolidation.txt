[[include:shared/fidelity]]

## 任务：提出候选表达偏好，不宣布全局规则
仅从有证据的实际改动中总结可操作的句法、连贯性或语气偏好。一次修改默认只适用于当前场景，不能推成全局禁词、固定句长、禁止把字句、删除连接词或永远加强语气的规则。
术语具体译法归术语库，不提取。没有足够证据时返回 NONE，不要求凑满条数。候选由程序保存并待人工确认，不能自称已经获批。

## 当前规则（仅整理建议，不直接删除已批准规则）
{rules_text}
合并确实同义且作用域相同的条目；不同情境的建议不算矛盾。不能以较新或较具体为由悄悄覆盖另一规则，不发明新规则，不将条件性建议改成绝对禁令。术语规则可标记待迁移，但不丢失原规则来源。
输出中文条目，每行以“- ”开头；无法安全归并则保留原条目。无有效条目输出 NONE。
