# Translation Agent Dependencies

# Core
pydantic>=2.0.0
pydantic-settings>=2.0.0
pyyaml>=6.0

# HTML Parsing
beautifulsoup4>=4.12.0
lxml>=4.9.0
markdownify>=1.2.0

# LLM
# Pin the validated SDK version. Newer/older releases have shown different
# import-time behavior on Python 3.14, so keep installs deterministic.
google-genai==1.66.0

# Web Framework
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.22

# Utilities
python-slugify>=8.0.0
python-dotenv>=1.0.0
tzdata>=2025.3
portalocker>=2.8.0  # File locking for concurrent access safety
